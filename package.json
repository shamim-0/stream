{
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "scripts": {
    "frontend": "cd stream && npm install && npm run dev",
    "backend": "cd server && npm install && npm run dev",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  }
}
